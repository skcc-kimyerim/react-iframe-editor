# FastAPI React Dev Server Requirements

# 핵심 의존성
fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<0.30
httpx>=0.25,<0.28

# 데이터 검증 및 직렬화
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0

# 추가 유용한 패키지 (선택사항)
python-multipart==0.0.6  # 파일 업로드 지원시 필요
aiofiles==23.2.1          # 비동기 파일 I/O (더 나은 성능을 위해)

# 개발 도구 (선택사항)
pytest==7.4.3            # 테스트
pytest-asyncio==0.21.1   # 비동기 테스트
black==23.11.0            # 코드 포맷터
flake8==6.1.0             # 린터